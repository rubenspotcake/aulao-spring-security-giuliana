spring:
    application:
        name: aulao-spring-security
    security:
      oauth2:
        client:
          registration:
            google:
              client-id: 
              client-secret: 
        resourceserver:
          jwt:
            issuer-uri: https://accounts.google.com

server:
  port: 8080
